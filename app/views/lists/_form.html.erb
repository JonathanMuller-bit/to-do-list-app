<%= form_with(model: list, local: true) do |form| %>
  <% if list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="mb-3">
      <label for="exampleInputEmail1" class="form-label"><%= form.label :description %></label>
      <%= form.text_field :description, class:'form-control' %>
    </div>
    <div class="mb-3 form-check">
      <input  class="form-check-input" id="exampleCheck1"><%= form.check_box :completed, class:'form-check-input' %>
      <label class="form-check-label" for="exampleCheck1"><%= form.label :completed %></label>
    </div>
    <button type="submit" class="btn btn-primary"><%= form.submit nil, class:'btn btn-primary' %></button>
  </form>

<% end %>
